{
  "name": "@quieter/deployment",
  "private": true,
  "type": "module",
  "scripts": {
    "fmt": "oxfmt --config ../../oxfmt.json .",
    "fmt:check": "oxfmt --check --config ../../oxfmt.json .",
    "lint": "bun --cwd ../.. oxlint --config oxlint.json packages/deployment",
    "lint:fix": "bun --cwd ../.. oxlint --fix --config oxlint.json packages/deployment",
    "typecheck": "tsgo --noEmit"
  },
  "devDependencies": {
    "@quieter/config": "workspace:*",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:"
  }
}
